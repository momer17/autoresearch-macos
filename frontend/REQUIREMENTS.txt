AUTORESEARCH FRONTEND REQUIREMENTS
===================================
Goal: A functional dashboard to start experiments and watch them run live.
No fancy design needed — clean, readable, working.

---

COMPLETED
---------
[x] src/lib/api.ts
      - Full TypeScript types matching real backend contracts
      - postStart() sends multipart/form-data (file upload + form fields)
      - getStatus() polls GET /status
      - API_BASE_URL constant — swap GPU IP here only

[x] src/pages/StartPage.tsx
      - File upload input (CSV)
      - target_col, task_description, metric, task_type, feature_cols, total_iterations
      - Sends FormData to POST /start
      - Loading + error states, disabled button styling

[x] src/App.tsx
      - Simple page state: "start" | "dashboard"
      - No router needed for 2 pages

[x] src/style.css
      - Minimal reset only (body margin, box-sizing, font, background)
      - All template CSS removed

---

TODO — Dashboard (next)
-----------------------
[ ] src/pages/DashboardPage.tsx
      On mount: poll getStatus() every 3 seconds via setInterval
      On unmount: clear interval
      Stop polling when status === "complete" or "error"
      Show loading state before first response arrives
      Show connection error inline if fetch throws (keep polling)

[ ] Status badge
      idle / setting_up / running_baseline / running → show as "Running..."
      complete → green
      error → red, show state.error message

[ ] Score panel
      Baseline score vs best score so far
      Improvement delta (formatted to 4 decimal places)
      Note: metric direction varies — higher is better for f1/accuracy/roc_auc/r2,
            lower is better for rmse/mae. Display accordingly.

[ ] Progress bar
      current_iteration / total_iterations
      Label: "Iteration 3 / 8"
      Hide during setting_up and running_baseline phases

[ ] Research summary
      Show research_summary text
      During setup phases show the live status string (e.g. "Running research...")
      Collapsible once experiment is running

[ ] Iteration table
      Columns: # | Score | Best | Strategy | Kept | Error
      Most recent at TOP
      Score: show "—" if null (crashed run)
      Kept: ✓ green / ✗ red
      Strategy: truncate with full text on hover (title attribute)
      Error: show short error if present
      Scrollable (max-height + overflow-y: auto)

---

API CONTRACTS (as built)
------------------------
POST /start — multipart/form-data
  file             File      required — CSV upload
  target_col       string    required
  task_description string    required
  metric           string    required  (f1 | accuracy | roc_auc | rmse | mae | r2)
  task_type        string    required  (binary_classification | multiclass_classification | regression)
  feature_cols     string    optional  comma-separated, empty = all except target
  total_iterations number    optional  default 8

GET /status — JSON
  status           "idle" | "setting_up" | "running_baseline" | "running" | "complete" | "error"
  experiment_id    string | null
  baseline         number | null
  best_score       number | null
  current_iteration number
  total_iterations  number
  research_summary  string
  iterations        Iteration[]
  error             string | null

Iteration {
  iteration   number
  score       number | null   (null = crashed run)
  best_score  number
  strategy    string
  kept        boolean
  error       string | null
}

---

FILE STRUCTURE
--------------
src/
  lib/
    api.ts              ← DONE
  pages/
    StartPage.tsx       ← DONE
    DashboardPage.tsx   ← stub, needs full implementation
  components/           ← create as needed during dashboard work
  App.tsx               ← DONE
  main.tsx              ← DONE
  style.css             ← DONE (minimal reset)

---

KEY RULES
---------
1. API_BASE_URL in api.ts is the only place to set the GPU IP.
2. No mock data — backend is live.
3. Poll every 3 seconds. Stop on "complete" or "error".
4. Metric direction: higher is better for f1/accuracy/roc_auc/r2 — lower for rmse/mae.
5. score in iterations can be null — always null-check before displaying.
6. No UI libraries needed.
